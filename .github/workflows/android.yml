name: Android CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
      env:
        GOOGLE_SERVICES_JSON: ${{ secrets.google_services_json }}
    - name: Where am i
      run: ls
    - name: Create files
      run: echo $GOOGLE_SERVICES_JSON > Melon/app/google-services.json
    - name: Build with Gradle
      run: ./gradlew build
