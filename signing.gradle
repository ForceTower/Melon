ext {
    signFile = file("sign.jks")
    String contents = new File('keystore_auth').text
    String[] values = contents.split(";")
    signKeystorePassword = values[0]
    signKeyAlias = values[1]
    signKeyPassword = values[2]
}