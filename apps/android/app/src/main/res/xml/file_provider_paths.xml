<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Offline slots for the Comprovante / Histórico PDFs. -->
    <files-path
        name="documents"
        path="documents/" />
    <!-- Downloaded material attachments, handed to the system viewer. -->
    <cache-path
        name="materials"
        path="materials/" />
    <!-- Camera capture slot for the profile-photo flow. -->
    <cache-path
        name="camera"
        path="camera/" />
    <!-- Rendered message cards handed to the share chooser. -->
    <cache-path
        name="share"
        path="share/" />
</paths>
