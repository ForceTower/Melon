<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Melon" parent="android:Theme.DeviceDefault.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>

    <!-- Cold-start splash (androidx.core:core-splashscreen). The launcher
         Activity boots into this; installSplashScreen() hands off to
         Theme.Melon once the first frame is ready. -->
    <style name="Theme.Melon.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="windowSplashScreenAnimationDuration">600</item>
        <item name="postSplashScreenTheme">@style/Theme.Melon</item>
    </style>
</resources>
